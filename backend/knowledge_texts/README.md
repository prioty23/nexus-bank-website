Place PDF-extracted EBL text files in this folder.

The chatbot searches every `.txt` file here and adds relevant snippets to the
answer context. Use clear file names such as `Retail_fees.txt`,
`SME_fees.txt`, or `Card_fees.txt` so file-name keywords help retrieval.
